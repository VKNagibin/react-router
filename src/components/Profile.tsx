import React from "react";
import styled from "styled-components";

const Profile = (): JSX.Element => {
    return (
       <h1>This page only for authorized users</h1>
    )
}

export default Profile;
